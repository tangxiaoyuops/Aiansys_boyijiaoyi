# 工具调用错误修复报告

## 🐛 错误信息

```json
{
  "status": "error",
  "error": "'int' object is not iterable",
  "stock_code": "603698"
}
```

## 📍 错误位置

**文件：** `core/agent_framework/examples/boyi_real_tools.py`
**函数：** `calculate_real_indicators`
**行号：** 110-112

## 🔍 错误原因

代码错误地将**数组**传给了期望**DataFrame**的函数：

```python
# ❌ 错误代码
close_prices = df['收盘'].values  # 提取为数组
ma5 = calculate_ma(close_prices, 5)  # 传入数组，但函数期望DataFrame
```

但 `calculate_ma` 函数签名是：
```python
def calculate_ma(data: pd.DataFrame, periods: list = [5, 10, 20, 60]) -> pd.DataFrame:
    """期望DataFrame作为输入"""
```

## ✅ 修复方案

修改 `calculate_real_indicators` 函数，使用正确的方式调用技术指标计算函数：

### 修复前（错误）
```python
# 提取为数组
close_prices = df['收盘'].values

# 错误调用
ma5 = calculate_ma(close_prices, 5)  # ❌ 传入int，函数期望list
ma20 = calculate_ma(close_prices, 20)  # ❌ 传入int，函数期望list
ma60 = calculate_ma(close_prices, 60)  # ❌ 传入int，函数期望list

# 返回值处理也错误
"ma5": float(ma5[-1]) if len(ma5) > 0 else 0
```

### 修复后（正确）
```python
# 直接使用DataFrame
df_ma = calculate_ma(df, periods=[5, 20, 60])

# 正确访问结果
"ma5": float(df_ma.iloc[-1]['MA5']) if 'MA5' in df_ma.columns else 0,
"ma20": float(df_ma.iloc[-1]['MA20']) if 'MA20' in df_ma.columns else 0,
"ma60": float(df_ma.iloc[-1]['MA60']) if 'MA60' in df_ma.columns else 0,
```

## 📝 完整修复代码

**文件：** `core/agent_framework/examples/boyi_real_tools.py`
**行号：** 95-130

```python
try:
    # 获取K线数据
    df = fetch_stock_data(stock_code, days)
    
    if df.empty:
        return {
            "status": "error",
            "error": f"无法获取股票 {stock_code} 的数据"
        }
    
    # 计算技术指标
    indicators = {
        "current_price": float(df.iloc[-1]['收盘']),
        "volume": float(df.iloc[-1]['成交量']) if '成交量' in df.columns else 0,
    }
    
    # 均线 - 使用DataFrame
    df_ma = calculate_ma(df, periods=[5, 20, 60])
    indicators.update({
        "ma5": float(df_ma.iloc[-1]['MA5']) if 'MA5' in df_ma.columns and not df_ma.empty else 0,
        "ma20": float(df_ma.iloc[-1]['MA20']) if 'MA20' in df_ma.columns and not df_ma.empty else 0,
        "ma60": float(df_ma.iloc[-1]['MA60']) if 'MA60' in df_ma.columns and not df_ma.empty else 0,
    })
    
    # MACD - 使用DataFrame
    df_macd = calculate_macd(df)
    indicators.update({
        "macd": float(df_macd.iloc[-1]['MACD']) if 'MACD' in df_macd.columns and not df_macd.empty else 0,
        "macd_signal": float(df_macd.iloc[-1]['MACD_Signal']) if 'MACD_Signal' in df_macd.columns and not df_macd.empty else 0,
        "macd_hist": float(df_macd.iloc[-1]['MACD_Hist']) if 'MACD_Hist' in df_macd.columns and not df_macd.empty else 0,
        "macd_positive": bool(df_macd.iloc[-1]['MACD_Hist'] > 0) if 'MACD_Hist' in df_macd.columns and not df_macd.empty else False,
    })
    
    # RSI - 使用DataFrame
    df_rsi = calculate_rsi(df)
    indicators["rsi"] = float(df_rsi.iloc[-1]['RSI']) if 'RSI' in df_rsi.columns and not df_rsi.empty else 50
```

## 🎯 错误原因分析

1. **类型不匹配**
   - 函数期望：`pd.DataFrame`
   - 实际传入：`int` (periods参数)

2. **参数错误**
   - 函数期望：`periods=[5, 20, 60]` (列表)
   - 实际传入：`5` (整数)
   - Python尝试迭代整数：`for period in 5` → 报错

3. **返回值处理错误**
   - 函数返回：DataFrame
   - 代码期望：数组

## 🔄 正确的调用方式

| 函数 | 输入类型 | 返回类型 | 正确调用 |
|------|---------|---------|---------|
| `calculate_ma` | DataFrame | DataFrame | `df_ma = calculate_ma(df, periods=[5,20,60])` |
| `calculate_macd` | DataFrame | DataFrame | `df_macd = calculate_macd(df)` |
| `calculate_rsi` | DataFrame | DataFrame | `df_rsi = calculate_rsi(df)` |

## ✅ 修复验证

修复后，工具调用应该返回：

```json
{
  "status": "success",
  "current_price": 25.30,
  "volume": 1234567,
  "ma5": 24.85,
  "ma20": 24.20,
  "ma60": 23.50,
  "macd": 0.15,
  "macd_signal": 0.12,
  "macd_hist": 0.03,
  "macd_positive": true,
  "rsi": 58.5,
  "recent_gain_20d": 2.5,
  "recent_gain_60d": 8.3,
  "recent_gain_120d": 15.2,
  "max_drawdown_60d": -5.2,
  "volatility_20d": 12.5,
  "message": "技术指标计算成功"
}
```

## 🚀 下一步操作

1. **重启后端服务**
   ```powershell
   python -m uvicorn server.app:app --host 0.0.0.0 --port 8000
   ```

2. **测试工具调用**
   - 配置Agent
   - 输入分析任务
   - 观察 `calc_indicators` 工具执行

3. **预期结果**
   - ✅ 工具成功执行
   - ✅ 返回完整的技术指标
   - ✅ 不再出现 "'int' object is not iterable" 错误

## 📊 影响范围

- ✅ 修复了 `calculate_real_indicators` 函数
- ✅ 影响所有使用 `calc_indicators` 工具的Agent分析
- ✅ 提高了数据处理的正确性
- ✅ 确保技术指标计算的准确性

---

**修复完成！重启服务后Agent应该能正常计算技术指标了。**
