# 选股策略前端使用指南

## 🎯 重构后的变化

### 统一使用GameTheoryStrategy

**选股** 和 **回测** 现在使用完全相同的博弈交易策略！

```
┌─────────────────────────────────┐
│   GameTheoryStrategy (核心)     │
│  - 恐慌点识别                    │
│  - 阶段判断                      │
│  - 信号生成                      │
└─────────────────────────────────┘
         ↓              ↓
    ┌─────────┐    ┌─────────┐
    │  选股   │    │  回测   │
    └─────────┘    └─────────┘
```

---

## 🚀 快速开始

### 1. 启动服务

**后端**:
```bash
python -m uvicorn server.app:app --reload --port 8000
```

**前端**:
```bash
cd frontend
npm run dev
```

### 2. 访问界面

浏览器打开: **http://localhost:5173/selection**

---

## 📊 界面说明

### 配置选择区域

选择三种配置之一:

- **标准配置**: 最多10只,单只≤20%
- **保守型配置**: 最多5只,单只≤15%  
- **激进型配置**: 最多15只,单只≤25%

这些参数会传递给GameTheoryStrategy,影响买入信号生成。

### 股票池设置

**方式1: 快速填充**（推荐新手）
```
点击"快速填充测试股票"按钮
→ 自动填充10只常用股票
→ 点击"开始选股"
```

**方式2: 手动输入**
```
输入股票代码,用逗号或换行分隔:
600519, 000001, 600036

或

600519
000001
600036
```

**方式3: 预设股票池**
```
点击"预设股票池"标签
→ 选择"测试股票池"(10只)
→ 点击"开始选股"
```

---

## 📈 查看结果

### 选股结果

**统计卡片**:
- 扫描股票: 总共分析了几只
- 合格股票: 有买入信号的股票
- 推荐股票: 最终推荐的股票

**推荐股票列表**:
```
┌────────────────────────────┐
│ 1. 贵州茅台 (600519)      │
│ 评分: 85.3 | 二阶段       │
│                            │
│ 买入信号: 3个             │
│ 当前价格: 1650.00         │
│ 建议仓位: 18%             │
│ 建议买入: 109股           │
└────────────────────────────┘
```

**展开查看详细**:
- 买入信号列表（所有历史买点）
- 最新买入信号详情
- 仓位建议

### 单只股票分析

```
输入股票代码 (如: 600519)
→ 点击"分析"
→ 查看K线图和买入信号
```

**K线图展示**:
- 标准蜡烛图
- 成交量柱状图
- 买入信号标记（三角形）
  - 🔴 恐慌点
  - 🔵 O点
  - 🟠 洗盘结束
  - 🟢 其他买入信号

---

## 🔄 工作流程

### 完整流程图

```
开始
 ↓
选择配置类型
 ↓
设置股票池
 ├─ 手动输入
 ├─ 快速填充
 └─ 预设股票池
 ↓
点击"开始选股"
 ↓
后端处理:
 ├─ 创建GameTheoryStrategy
 ├─ 遍历每只股票
 ├─ generate_signal()找买点
 ├─ 收集买入信号
 └─ 综合评分筛选
 ↓
返回推荐股票
 ↓
查看结果
 ├─ 统计数据
 ├─ 推荐列表
 └─ 详细信息
 ↓
单只股票分析 (可选)
 ├─ K线图展示
 └─ 买点标记
 ↓
结束
```

---

## 🎨 界面特点

### 响应式设计

- **桌面端**: 多列布局,信息丰富
- **移动端**: 单列布局,操作简便

### 交互友好

- 点击卡片展开详情
- 实时显示输入数量
- 加载动画提示
- 错误友好提示

### 视觉清晰

- 配色符合金融系统风格
- 阶段标识彩色徽章
- 评分可视化进度条
- K线图专业渲染

---

## 💡 实用技巧

### 技巧1: 提高选股成功率

```
✅ 股票池5-30只（太多太慢,太少不准）
✅ 选择热门板块股票
✅ 关注大盘环境
✅ 分批建仓,控制仓位
```

### 技巧2: 理解买入信号

```
恐慌点买入: 
- 大阴线+放量下跌
- 阶段1-2优先
- 风险较高,收益较大

O点买入:
- 长期下跌后的起点
- 地量确认
- 安全性较高

洗盘结束:
- 二阶段回调后
- 成交量萎缩
- 趋势延续
```

### 技巧3: 验证买入时机

```
1. 查看K线图上的标记位置
2. 判断是否在合理价位
3. 检查阶段是否合适
4. 结合大盘环境决策
```

---

## 🔧 参数说明

### 策略参数

前端会根据配置类型自动设置:

```javascript
// 标准配置
{
  max_stocks: 10,
  max_position_per_stock: 0.2,
  initial_capital: 1000000,
  data_days: 250
}

// 保守型配置
{
  max_stocks: 5,
  max_position_per_stock: 0.15,
  ...
}

// 激进型配置
{
  max_stocks: 15,
  max_position_per_stock: 0.25,
  ...
}
```

### 博弈策略参数

这些参数传递给GameTheoryStrategy:

```python
# 恐慌点参数
panic_drop_threshold: -3.0  # 跌幅阈值
panic_vol_ratio: 1.5        # 放量倍数
panic_window: 60            # 检测窗口

# 阶段判断
stage_window: 60            # 判断窗口
```

---

## 📊 数据流程

### 前端 → 后端

```javascript
// 选股请求
POST /api/selection/select
{
  "codes": ["600519", "000001"],
  "strategy_params": {
    "max_stocks": 10,
    "max_position_per_stock": 0.2
  }
}

// 单只股票分析
POST /api/selection/analyze
{
  "code": "600519",
  "days": 250
}
```

### 后端处理

```python
# 使用GameTheoryStrategy
strategy = GameTheoryStrategy()
strategy.initialize(params)

# 遍历历史数据
for i in range(120, len(df)):
    signal = strategy.generate_signal(data, portfolio)
    if signal:
        buy_signals.append(signal)

# 返回结果
return {
  "top_candidates": [...],
  "kline_data": [...],
  "buy_signals": [...]
}
```

### 前端展示

```javascript
// 接收结果
selectionResult.value = response.data

// 显示推荐股票
candidate.buy_signals  // 买入信号列表
candidate.best_signal // 最新买入信号
candidate.overall_score // 综合评分

// 显示K线图
klineData: response.data.kline_data
buySignals: response.data.buy_signals
```

---

## ⚠️ 注意事项

### 常见问题

**Q: 为什么没有推荐股票?**
```
A: 可能原因:
1. 股票池太小
2. 当前没有买点
3. 筛选条件太严

解决:
- 扩大股票池
- 等待时机
- 调整配置
```

**Q: 选股很慢怎么办?**
```
A: 原因: 需要遍历历史数据

建议:
- 控制股票池10-20只
- 检查网络连接
- 耐心等待10-30秒
```

**Q: 如何验证结果?**
```
A: 使用单只股票分析:
1. 输入推荐股票代码
2. 点击"分析"
3. 查看K线图上的买点标记
4. 判断是否合理
```

---

## 📝 最佳实践

### 每日流程

```
9:00-9:30  查看昨日选股结果
9:30-11:30 监控开盘情况
11:30-13:00 午间复盘
13:00-15:00 盘中决策
15:00-16:00 运行选股
16:00-17:00 研究推荐股票
```

### 风险控制

```
✅ 单只股票 ≤ 20% 仓位
✅ 总持仓 ≤ 10 只
✅ 严格执行止损(8%)
✅ 关注大盘环境
✅ 分批建仓
```

---

## 🎯 下一步

1. **立即测试**: 点击"快速填充测试股票" → "开始选股"
2. **查看K线**: 在单只股票分析中输入代码查看详细
3. **参数调优**: 尝试不同配置类型对比结果
4. **实战应用**: 结合实际交易,逐步优化

---

**记住: 选股和回测使用相同的博弈交易策略,逻辑完全一致！** 🌟